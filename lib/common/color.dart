import 'package:flutter/animation.dart';
import 'package:flutter/material.dart';

/// CustomColors class to declare required customized colors
class MyColors {
  static const themeColor = Color.fromRGBO(34, 82, 160, 1);
  static const bookmarkColor = Colors.green;
  static const dictationColor = Colors.red;
  static const dividerColor = Colors.grey;
  static const dictationTextColor = Colors.black;
}